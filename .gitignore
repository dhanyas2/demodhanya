# Terraform cache and downloads
**/.terraform/

# State files (never commit)
*.tfstate
*.tfstate.*

# Plans and logs
*.tfplan
crash.log
crash.*.log

# Local var files (often contain secrets)
*.tfvars
*.tfvars.json

# Overrides (local-only)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI config
.terraformrc
terraform.rc
